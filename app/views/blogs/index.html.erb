<% content_for :left_menu do%>
  <p>Primairy Selector goes here</p>
<%end%>

<% content_for :right_menu do%>
  <p>Secondary Selector goes here</p>
<%end%>

<div class="content" id="blogs">
  <div class="title"><span>Blog Hop</span></div>
  <p>Here are the blogs that you have subscirbed to!</p>
  <div class="fr">  <%= link_to 'Subscirbe to some Blogs', feeds_path %></div>
  <div class="blogs">
      <% @blogs.each do |blog| %>
      <div class="blog">
        <div class="blog_title fl">
          <div class="fl" id="title"><%= link_to blog.title, blog_path(blog) %> </div>
        </div>

        <div class="fr blog_origins">
          <%= blog.feed.name%>
        </div>
          <span style="margin: 0 0 5px 10px; " class="fl">Posted: <%= blog.published_at %></span>
        <div class='clear'></div>

        <div class="blog_info">
          <span class="fl"><%= pluralize(blog.trend_count, "person", "people") %> have trended this post. </span>
           <!-- <%# case blog.trended?(current_user) %>
              <%#when 1%>
                <%#= link_to untrend_posts_path(:post_id => blog.id) do%>
                  <div role="button" class="untrend-button">Un-Trend</div>
                <%#end%>
              <%#when 2%>
                <%#= link_to retrend_posts_path(:post_id => @post1.id) do%>
                  <div role="button" class="retrend-button">Re-Trend</div>
                <%# end%>
            <%#end%> -->
          <span class="fl"><%= link_to "View Original Article", "#{blog.url}" %> </span>

        </div>
        <div class="blog_content">
          <p><%= raw(blog.summary) %></p>
        </div>
        <div class="blog_trends">
          <span>Trends in this blog: </span>
          <%# blog.trends.each do |trend| %>
            <span> <%#= link_to trend.name, trend_path(trend.name) %></span>
          <%# end%>
        </div>
      </div>
      <% end %>
  </div>
</div>