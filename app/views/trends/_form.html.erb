<%= form_for(@trend) do |f| %>
  <% if @trend.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trend.errors.count, "error") %> prohibited this trend from being saved:</h2>

      <ul>
      <% @trend.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <p>Title </p>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :trend_category_id %><br />
    <%= f.number_field :trend_category_id %>
  </div>
  <div class="field">
    <p>What's Trending? </p>
    <%= f.text_area  :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
